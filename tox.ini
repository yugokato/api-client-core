[tox]
env_list =
    linting
    py{311,312,313,314}

[testenv]
deps =
    py,py{311,312,313,314}: .[dev]
commands = pytest {posargs:-n auto}

[testenv:linting]
skip_install = True
allowlist_externals =
    pre-commit
commands = pre-commit run --all-files --show-diff-on-failure
